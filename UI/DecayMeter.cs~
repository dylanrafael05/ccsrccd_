using System;
using Terraria;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using Terraria.UI;

namespace ChemistryClass.UI {
    public class DecayMeter : UIElement {

        public float displayPurity = 1f;
        public int length = 5;

        public const float scale = 2f;
        public const int frameSize = 12;
        public const int barYOffset = 5;
        public const int barScale = 2;

        public override void Draw(SpriteBatch spriteBatch) {

            Player plr = Main.LocalPlayer;

            Vector2 drawStart =
                plr.bodyFrame.Center.ToVector2() +
                new Vector2(-scale * 12, -plr.bodyFrame.Height / 2 - Main.screenHeight / 6f);

            Vector2 offset

        }

    }
}
